import { useState } from "react";
import { Bell, Check, ChevronDown, KeyRound, Link2, LockKeyhole, Moon, Monitor, Save, ShieldCheck, UserRound } from "lucide-react";
import { Button, SectionEyebrow, useToast } from "@/components/Shared";

function Toggle({ enabled, onChange }: { enabled: boolean; onChange: (value: boolean) => void }) { return <button onClick={() => onChange(!enabled)} role="switch" aria-checked={enabled} className={`relative h-5 w-9 shrink-0 rounded-full transition ${enabled ? "bg-[#b8e94f]" : "bg-white/15"}`}><span className={`absolute top-0.5 h-4 w-4 rounded-full transition ${enabled ? "left-[18px] bg-[#101412]" : "left-0.5 bg-[#9da8a1]"}`} /></button>; }
function SettingsSection({ icon: Icon, eyebrow, title, description, children }: { icon: typeof UserRound; eyebrow: string; title: string; description: string; children: React.ReactNode }) { return <section className="panel p-5 lg:p-6"><div className="flex gap-4"><span className="flex h-9 w-9 shrink-0 items-center justify-center border border-white/10 bg-white/[.04] text-[#b8e94f]"><Icon size={16} /></span><div className="min-w-0 flex-1"><SectionEyebrow>{eyebrow}</SectionEyebrow><h2 className="mt-2 font-display text-lg font-semibold text-white">{title}</h2><p className="mt-2 max-w-2xl text-xs leading-5 text-[#7f8a84]">{description}</p>{children}</div></div></section>; }

export default function Settings() {
  const { notify } = useToast();
  const [emailAlerts, setEmailAlerts] = useState(true); const [weeklyDigest, setWeeklyDigest] = useState(true); const [highRisk, setHighRisk] = useState(true); const [autoDelete, setAutoDelete] = useState(false); const [saved, setSaved] = useState(false);
  const save = () => { setSaved(true); notify("Settings saved", "Your workspace preferences were updated locally.", "success"); window.setTimeout(() => setSaved(false), 2500); };
  return <div className="mx-auto max-w-[1000px] animate-page"><div className="mb-8 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><SectionEyebrow>Workspace / control room</SectionEyebrow><h1 className="mt-3 font-display text-3xl font-bold tracking-[-.05em] text-white lg:text-[38px]">Settings<span className="text-[#b8e94f]">.</span></h1><p className="mt-2 text-sm leading-6 text-[#84908a]">Tune how VoiceGuard fits into your review workflow.</p></div><Button onClick={save}>{saved ? <Check size={15} /> : <Save size={15} />}{saved ? "Saved" : "Save changes"}</Button></div><div className="space-y-4"><SettingsSection icon={UserRound} eyebrow="Profile" title="Your workspace identity" description="This information is used across reports and team notifications."><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="block"><span className="mb-2 block text-[10px] font-semibold uppercase tracking-[.15em] text-[#6c7871]">Full name</span><input defaultValue="Jordan Davis" className="field" /></label><label className="block"><span className="mb-2 block text-[10px] font-semibold uppercase tracking-[.15em] text-[#6c7871]">Workspace</span><input defaultValue="Northstar Trust & Safety" className="field" /></label><label className="block sm:col-span-2"><span className="mb-2 block text-[10px] font-semibold uppercase tracking-[.15em] text-[#6c7871]">Email address</span><input defaultValue="jordan@northstar.co" className="field" /></label></div></SettingsSection><SettingsSection icon={Moon} eyebrow="Appearance" title="Interface preferences" description="Choose how your review environment looks and behaves."><div className="mt-5 flex flex-col gap-3 sm:flex-row"><button className="flex flex-1 items-center gap-3 border border-[#b8e94f]/40 bg-[#b8e94f]/[.08] p-3 text-left"><Moon size={16} className="text-[#b8e94f]" /><span><span className="block text-xs font-semibold text-white">Dark mode</span><span className="mt-1 block text-[10px] text-[#7f8a84]">Low-light workspace</span></span><Check size={14} className="ml-auto text-[#b8e94f]" /></button><button onClick={() => notify("Light mode is coming soon", "Dark mode is the recommended review environment.", "info")} className="flex flex-1 items-center gap-3 border border-white/10 p-3 text-left transition hover:border-white/25"><Monitor size={16} className="text-[#79857e]" /><span><span className="block text-xs font-semibold text-[#c6cec9]">System theme</span><span className="mt-1 block text-[10px] text-[#7f8a84]">Follow device settings</span></span></button></div></SettingsSection><SettingsSection icon={Bell} eyebrow="Notifications" title="Stay close to risk signals" description="Decide which events should reach your inbox."><div className="mt-5 divide-y divide-white/8">{[["Analysis complete", "Get a notification when a scan finishes.", emailAlerts, setEmailAlerts], ["High-risk signal detected", "Notify me when AI confidence exceeds 80%.", highRisk, setHighRisk], ["Weekly activity digest", "A Monday summary of workspace volume.", weeklyDigest, setWeeklyDigest]].map(([label, desc, value, setter]) => <div key={label as string} className="flex items-center justify-between gap-4 py-3 first:pt-0 last:pb-0"><div><p className="text-xs font-semibold text-[#dce4df]">{label as string}</p><p className="mt-1 text-xs text-[#77837c]">{desc as string}</p></div><Toggle enabled={value as boolean} onChange={setter as (value: boolean) => void} /></div>)}</div></SettingsSection><SettingsSection icon={ShieldCheck} eyebrow="Analysis preferences" title="Default screening behavior" description="Set sensible defaults for new files without changing the underlying detection model."><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="block"><span className="mb-2 block text-[10px] font-semibold uppercase tracking-[.15em] text-[#6c7871]">Default sensitivity</span><div className="relative"><select className="field appearance-none"><option>Balanced</option><option>Conservative</option><option>High sensitivity</option></select><ChevronDown size={14} className="pointer-events-none absolute right-3 top-1/2 -translate-y-1/2 text-[#75817a]" /></div></label><label className="block"><span className="mb-2 block text-[10px] font-semibold uppercase tracking-[.15em] text-[#6c7871]">Retention window</span><div className="relative"><select className="field appearance-none"><option>90 days</option><option>30 days</option><option>1 year</option></select><ChevronDown size={14} className="pointer-events-none absolute right-3 top-1/2 -translate-y-1/2 text-[#75817a]" /></div></label><div className="flex items-center justify-between gap-4 border border-white/8 bg-white/[.02] p-3 sm:col-span-2"><div><p className="text-xs font-semibold text-[#dce4df]">Auto-delete source audio</p><p className="mt-1 text-xs text-[#77837c]">Remove originals after a result is generated.</p></div><Toggle enabled={autoDelete} onChange={setAutoDelete} /></div></div></SettingsSection><SettingsSection icon={LockKeyhole} eyebrow="Security" title="Workspace access" description="Review local access settings for this prototype environment."><div className="mt-5 flex flex-col gap-3 sm:flex-row"><button onClick={() => notify("Password flow is coming soon", "Authentication is intentionally mocked in this frontend-only build.", "info")} className="flex flex-1 items-center gap-3 border border-white/10 p-3 text-left transition hover:border-white/25"><KeyRound size={16} className="text-[#b8e94f]" /><span><span className="block text-xs font-semibold text-white">Change password</span><span className="mt-1 block text-[10px] text-[#7f8a84]">Last changed 24 days ago</span></span></button><button onClick={() => notify("Session review opened", "No unfamiliar sessions found in this mock workspace.", "success")} className="flex flex-1 items-center gap-3 border border-white/10 p-3 text-left transition hover:border-white/25"><Link2 size={16} className="text-[#6ee7c8]" /><span><span className="block text-xs font-semibold text-white">Review active sessions</span><span className="mt-1 block text-[10px] text-[#7f8a84]">1 current session</span></span></button></div></SettingsSection></div></div>;
}
